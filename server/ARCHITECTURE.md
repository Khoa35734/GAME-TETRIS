# 🎨 Authentication Flow Diagram

## 📊 System Architecture

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Client    │         │   Server    │         │  Database   │
│  (React)    │         │  (Express)  │         │ (Postgres)  │
└──────┬──────┘         └──────┬──────┘         └──────┬──────┘
       │                       │                       │
```

## 🔐 Registration Flow

```
Client                  Server                    Database
  │                       │                         │
  │  POST /auth/register  │                         │
  ├──────────────────────>│                         │
  │  {username, email,    │                         │
  │   password}           │                         │
  │                       │                         │
  │                       │  Check email exists     │
  │                       ├────────────────────────>│
  │                       │<────────────────────────┤
  │                       │  (No duplicate)         │
  │                       │                         │
  │                       │  Hash password          │
  │                       │  (bcrypt, 10 rounds)    │
  │                       │                         │
  │                       │  INSERT INTO account    │
  │                       ├────────────────────────>│
  │                       │<────────────────────────┤
  │                       │  (New user created)     │
  │                       │                         │
  │                       │  Generate JWT token     │
  │                       │  (7 days expiry)        │
  │                       │                         │
  │<──────────────────────┤                         │
  │  {success, token,     │                         │
  │   user}               │                         │
  │                       │                         │
  │  Store in localStorage│                         │
  │  - tetris:token       │                         │
  │  - tetris:user        │                         │
  │                       │                         │
```

## 🔑 Login Flow

```
Client                  Server                    Database
  │                       │                         │
  │  POST /auth/login     │                         │
  ├──────────────────────>│                         │
  │  {email, password}    │                         │
  │                       │                         │
  │                       │  SELECT from account    │
  │                       │  WHERE email = ?        │
  │                       ├────────────────────────>│
  │                       │<────────────────────────┤
  │                       │  (User data + hash)     │
  │                       │                         │
  │                       │  Compare password       │
  │                       │  bcrypt.compare()       │
  │                       │  ✅ Match!              │
  │                       │                         │
  │                       │  Generate JWT token     │
  │                       │                         │
  │<──────────────────────┤                         │
  │  {success, token,     │                         │
  │   user}               │                         │
  │                       │                         │
```

## ✅ Token Verification Flow

```
Client                  Server                    Database
  │                       │                         │
  │  GET /auth/verify     │                         │
  ├──────────────────────>│                         │
  │  Header: Bearer token │                         │
  │                       │                         │
  │                       │  Verify JWT signature   │
  │                       │  Check expiry           │
  │                       │  ✅ Valid!              │
  │                       │                         │
  │                       │  SELECT from account    │
  │                       │  WHERE account_id = ?   │
  │                       ├────────────────────────>│
  │                       │<────────────────────────┤
  │                       │  (User still exists)    │
  │                       │                         │
  │<──────────────────────┤                         │
  │  {success, user}      │                         │
  │                       │                         │
```

## 🎮 Game Integration Flow

```
┌──────────────────────────────────────────────────┐
│ 1. User opens app                                │
│    └─> Check localStorage for token             │
│        ├─> Token exists? → Verify with server   │
│        │   ├─> Valid? → Auto login ✅            │
│        │   └─> Invalid? → Show login form       │
│        └─> No token? → Show login/register form │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ 2. User clicks "Play Ranked"                     │
│    └─> Get token from localStorage               │
│        ├─> Send to server with accountId         │
│        └─> Server uses accountId for matchmaking │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ 3. Match ends                                     │
│    └─> Server updates:                           │
│        ├─> games_played++                        │
│        ├─> games_won++ (if won)                  │
│        ├─> elo_rating (ELO calculation)          │
│        └─> last_login (keep active)              │
└──────────────────────────────────────────────────┘
```

## 🗄️ Database Relations (Current)

```
┌────────────────────────┐
│       account          │
├────────────────────────┤
│ account_id (PK)        │◄───┐
│ username (UNIQUE)      │    │ Future: Match History
│ email (UNIQUE)         │    │ Foreign Key
│ password (HASHED)      │    │
│ created_at             │    │
│ updated_at             │    │
│ last_login             │    │
│ is_active              │    │
│ elo_rating             │    │
│ games_played           │    │
│ games_won              │    │
│ games_lost             │    │
└────────────────────────┘    │
                              │
┌────────────────────────┐    │
│    match_history       │    │
├────────────────────────┤    │
│ match_id (PK)          │    │
│ player1_id (FK)        ├────┘
│ player2_id (FK)        │
│ winner_id (FK)         │
│ duration               │
│ final_scores           │
│ created_at             │
└────────────────────────┘
   (Future table)
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│ 1. HTTPS (Transport Layer)              │ ← Production
├─────────────────────────────────────────┤
│ 2. CORS (Cross-Origin Protection)       │ ✅
├─────────────────────────────────────────┤
│ 3. JWT (Authentication)                 │ ✅
├─────────────────────────────────────────┤
│ 4. bcrypt (Password Hashing)            │ ✅
├─────────────────────────────────────────┤
│ 5. Sequelize (SQL Injection Protection) │ ✅
├─────────────────────────────────────────┤
│ 6. Input Validation                     │ ✅
├─────────────────────────────────────────┤
│ 7. Rate Limiting                        │ ← TODO
├─────────────────────────────────────────┤
│ 8. Email Verification                   │ ← TODO
└─────────────────────────────────────────┘
```

## 📊 Data Flow Example

### New User Journey

```
1. User visits site
   └─> Sees Login/Register

2. Clicks "Register"
   └─> Fills form: username, email, password
   └─> Client sends POST /auth/register
   └─> Server validates & hashes password
   └─> Database creates new account
   └─> Server generates JWT token
   └─> Client receives token + user data
   └─> Client stores in localStorage

3. User clicks "Play"
   └─> Client reads token from localStorage
   └─> Client connects to game with accountId
   └─> Server validates token
   └─> Game starts with authenticated user

4. Match ends
   └─> Server updates user stats in database
   └─> Client can view updated profile
```

## 🎯 Token Structure

```javascript
// JWT Payload
{
  accountId: 123,           // User ID
  email: "user@mail.com",  // User email
  username: "player1",      // Username
  iat: 1696800000,         // Issued at (timestamp)
  exp: 1697404800          // Expires at (timestamp, +7 days)
}

// Encoded as:
// header.payload.signature
// eyJhbGc...  .eyJhY2Nv...  .SflKxwRJ...
```

## 🔄 Token Lifecycle

```
┌──────────────┐
│ User Login   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Generate JWT │─────┐
│ (7 days)     │     │
└──────┬───────┘     │
       │             │
       ▼             │
┌──────────────┐     │
│ Send to      │     │
│ Client       │     │
└──────┬───────┘     │
       │             │
       ▼             │
┌──────────────┐     │
│ Store in     │     │
│ localStorage │     │
└──────┬───────┘     │
       │             │
       ▼             │
┌──────────────┐     │
│ Use for API  │     │
│ Requests     │     │
└──────┬───────┘     │
       │             │
       │  7 days     │
       │  later...   │
       ▼             │
┌──────────────┐     │
│ Token        │     │
│ Expired!     │◄────┘
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Login Again  │
└──────────────┘
```

## 📱 Client Storage

```javascript
// localStorage structure
{
  "tetris:token": "eyJhbGciOi...",  // JWT token
  "tetris:user": {                   // User info (JSON string)
    "accountId": 123,
    "username": "player1",
    "email": "player@mail.com"
  }
}
```

## 🔧 Error Handling Flow

```
Client Request
     │
     ▼
  Server
     │
     ├─ Validation Error?
     │  └─> 400 Bad Request
     │      └─> {success: false, message: "..."}
     │
     ├─ Not Found?
     │  └─> 404 Not Found
     │      └─> {success: false, message: "..."}
     │
     ├─ Auth Error?
     │  └─> 401 Unauthorized
     │      └─> {success: false, message: "..."}
     │
     ├─ Server Error?
     │  └─> 500 Internal Error
     │      └─> {success: false, message: "..."}
     │
     └─ Success!
        └─> 200 OK / 201 Created
            └─> {success: true, data: {...}}
```

## 🎨 Component Integration

```
┌──────────────────────────────────────┐
│          App Component               │
│                                      │
│  ┌────────────────────────────────┐ │
│  │   AuthContext / AuthProvider   │ │
│  │  - currentUser                 │ │
│  │  - token                       │ │
│  │  - login()                     │ │
│  │  - register()                  │ │
│  │  - logout()                    │ │
│  └────────────┬───────────────────┘ │
│               │                      │
│  ┌────────────┴───────────────────┐ │
│  │                                │ │
│  │  ┌──────────┐   ┌───────────┐ │ │
│  │  │  Login   │   │ Register  │ │ │
│  │  │  Form    │   │   Form    │ │ │
│  │  └──────────┘   └───────────┘ │ │
│  │                                │ │
│  │  ┌──────────────────────────┐ │ │
│  │  │    Protected Routes      │ │ │
│  │  │  - Game                  │ │ │
│  │  │  - Profile               │ │ │
│  │  │  - Leaderboard           │ │ │
│  │  └──────────────────────────┘ │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

---

**Visual Guide Complete!** 📊  
See other docs for implementation details.
