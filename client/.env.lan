# LAN Configuration
# Dùng file này khi muốn máy khác kết nối qua LAN
# Copy file này thành .env hoặc dùng: npm run dev -- --mode lan

# Server IP trong LAN (IP của máy chạy server)
VITE_API_URL=http://192.168.1.105:4000/api

