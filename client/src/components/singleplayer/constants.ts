// --- Input and movement ---
export const DAS_DELAY = 120; // ms
export const MOVE_INTERVAL: number = 40; // ms

// --- Lock logic ---
export const INACTIVITY_LOCK_MS = 750; // lock if idle on ground
export const HARD_CAP_MS = 3000; // hard cap on ground

// --- Panel sizes/positions base ---
export const PANEL_WIDTH = 120;
export const PANEL_OFFSET_Y = -8;
export const SIDE_GAP = 14;

// --- Board offsets ---
export const BOARD_SHIFT_X = 0;
export const BOARD_SHIFT_Y = -30;
