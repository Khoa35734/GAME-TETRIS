import { PANEL_WIDTH, SIDE_GAP } from './constants';

export const HOLD_SHIFT_X = 30;
export const HOLD_SHIFT_Y = 0;
export const NEXT_SHIFT_X = 50;
export const NEXT_SHIFT_Y = 0;

export const HOLD_OFFSET_X = PANEL_WIDTH + SIDE_GAP;
export const NEXT_OFFSET_X = PANEL_WIDTH + SIDE_GAP;

